import * as React from 'react';
import Plotr from '../components/Plotr';

/**
 * Think of this as the base App component in CRA
 * We can add some mobile detection and stuff here later
 * @extends React
 */
class Edit extends React.Component {
  render() {
    return <Plotr />;
  }
}

export default Edit;
